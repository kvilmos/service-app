{
  "name": "service-app",
  "version": "0.0.1",
  "type": "module",
  "workspaces": [
    "./client",
    "./server",
    "./shared"
  ],
  "scripts": {
    "dev:server": "bun run --cwd server dev",
    "dev:client": "bun run --cwd client dev",
    "dev": "bun --filter \"*\" dev"
  }
}
